#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    array<int,5>a;
    for(int i=0;i<5;i++){
        cin>>a.at(i);
    }
    for(int i=0;i<5;i++){
        cout<<a.at(i);
    }

    return 0;
}